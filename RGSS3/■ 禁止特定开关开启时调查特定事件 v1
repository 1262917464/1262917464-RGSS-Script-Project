#encoding:utf-8
#==============================================================================
# ■ 禁止特定开关开启时调查特定事件 v1 by 1262917464
#------------------------------------------------------------------------------
# 　说明：
#   事件名包含 BAN_EVENT_NAME 的
#   会在 BAN_SWITCH 开启时无法调查
#------------------------------------------------------------------------------
#   本脚本由1262917464编写，BSD 3-Clause “New” or “Revised” 授权
#==============================================================================

BAN_EVENT_NAME = "[D]"

BAN_SWITCH = 126

class Game_Event < Game_Character
  #--------------------------------------------------------------------------
  # ● 事件启动
  #--------------------------------------------------------------------------
  alias old_start start
  def start
    return if @event.name.include?(BAN_EVENT_NAME) && $game_switches[BAN_SWITCH]
    old_start
  end
end
